# 人工核实执行清单

> 来源：`docs/pending_items.md` §2 —— 这里只做一件事：把"只能由真人做"的项**按渠道重组**，
> 让同一个部门的几件事能一次问完。区号统一 `025`，号码台账见 `docs/contact_phones.md`。
> 登记处仍然是 `pending_items.md`（本文件是**执行副本**，做完请回去把对应项划掉）。

**核心原则**：页面上有 ≠ 电话能通 ≠ 内容仍然正确。
本项目已经教训过两次 —— 图书馆官网两处都写「学生 30 册 / 借期 60 天」，
电话一问实际是「本科 10 册 / 30 天」，**是官网页面过时了**（详见 `pending_items.md` A8）。
所以凡是涉及时间 / 金额 / 数量 / 电话 / 地点的数字，**都以人工核实为准**。

---

## 一、打电话（3 通电话能解决 7 件事）

### ☎️ 第 1 通：学工处 · 公寓管理科 `85864025`

| # | 要问的原话 | 答案填回 |
|---|---|---|
| B1 | 「请问现在是这个号码处理学生公寓事务吗？」（**先验证号码还有人接**，不通就问总机要新号） | `contact_phones.md` |
| A4 | 「《学生公寓管理规定》里关于**损坏赔偿、下水道疏通费自理、责任不清由寝室成员共同承担**，分别是第几条？我想引用原文条款」 | `dorm_repair` 的 `_meta.note` |
| — | 「**学生宿舍管理中心**这个部门还存在吗？还是已经并入学工处？」 | 若不存在，删除 `dorm_curfew` 顶层 `source` 字段（见 §三） |

### ☎️ 第 2 通：心理健康教育教研室 `85864040`

| # | 要问的原话 | 答案填回 |
|---|---|---|
| B1 | 「现在还是这个号码做心理咨询预约/咨询吗？」 | `contact_phones.md` |
| A2 | 「心理咨询中心的**5 个功能室分别叫什么名字**？位置是不是『大学生创新发展中心一楼西侧』？」 | `psych_counseling` 答案 + `_meta.note` |

### ☎️ 第 3 通：数智化处（报修相关）

| # | 要问的原话 | 答案填回 |
|---|---|---|
| B2 | 「`85864098` / `85864122` / `85864358` 这几个报修电话现在还能用吗？如果换了是哪个？」 | `contact_phones.md` + `xxh_*` 相关答案 |

> 号码若已变，**先记下来**，不要当场改语料 —— 回来跑一遍 `pytest` 再改。

---

## 二、微信里搜一下（2 分钟）

| # | 做什么 | 判定 | 填回 |
|---|---|---|---|
| A3 | 微信搜公众号「**心海导航**」 | ① 是否仍是官方号（看认证主体是不是学校）；② 菜单是否仍是「心理咨询 → 预约咨询」 | `psych_counseling` 答案；若已改名/停用，**必须改**，否则答案是错的 |

---

## 三、建议「不做」而不是「去核实」的一项（省下半小时）

| # | 原登记 | 我的建议 |
|---|---|---|
| A6 | 核实 6 个顶层 `source` 部门名（`学生宿舍管理中心` / `学生资助管理中心` / `后勤管理处` / `就业指导中心` / `分团委`…） | **别核**。顶层 `source` 是**死字段 —— 代码从头到尾不读它**，也不计入溯源统计（真正的溯源一律看 `_meta`）。逐条核实部门名是纯浪费。**正确做法是把这个字段删掉**，或保留但明说"仅供参考、不作溯源"。真要核，只需要核 `_meta` 里出现部门名的地方。 |

---

## 四、需要登录账号 / 只有你能做

| # | 事项 | 为什么必须你来 |
|---|---|---|
| C1 | **Hugging Face Spaces 在线 Demo**（方案见 `docs/deploy_hf_spaces.md`，111 行，从未实操） | 需要你的 HF 账号；沙箱能读不能写（无凭据） |
| C2 | Docker + Nginx 真实上线 | 需实测 `FAQ_CORS_ORIGINS` / `FAQ_TRUST_PROXY_HEADERS` —— **走 nginx 必须设 `1`**，否则全校共用一个 IP 桶 |
| C3 | 和风天气专属域名 | 部署 `.env` 是否真填了 `HEFENG_BASE_URL` / `HEFENG_GEO_URL` |
| C4 | `docker-compose.yml` 的 build 能否跑通 | 改成 `${VAR:-}` 语法后未实测 |

---

## 五、这些我来做（不用占你时间，列出来免得你重复劳动）

| # | 事项 | 说明 |
|---|---|---|
| D3 | 测试总数对数 | 我跑 `pytest -q` 即可（当前 **184 passed**，与 README 一致） |
| D1 | README 宣称「召回率@1 从 57% → 100%」的**出处核实** | 需要翻历史数据确认测试集规模与是否封闭 —— 我会去查 git 历史；若查不到出处，**建议改成一个有据可查的说法**，别留一个站不住的简历数字 |
| E1 | 语料里官方链接是否仍可访问 | 我可以批量 curl 一遍所有 `_meta.url`，列出失效的 |
| — | OMP 防护 / 日志健壮性 | 见 `docs/rerank_report.md` §10：`build_ranker` 的"优雅降级"在 OMP abort 下**失效**（不是异常，捕不住） |

---

## 六、做完之后

1. 回 `docs/pending_items.md` 把对应项划掉（保持登记处与实际一致）；
2. 改了语料就**必须**：跑 `pytest -q` + `python evaluate.py --show-error` + `ruff check .`，
   然后**重启后端**（json 不热重载）；
3. 若动到 `psych_counseling` / `dorm_repair` 的答案，记得顺带更新
   `docs/unsourced_intents.md`（跑 `python scripts/report_unsourced.py`）。
